name: qg_stock_take_app
description: A new Flutter project.
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.1.2 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_hooks: ^0.20.5
  flutter_speed_dial: ^7.0.0
  http: ^1.2.1
  path: ^1.8.3
  provider: ^6.1.2
  shared_preferences: ^2.2.3
  sqflite: ^2.3.0

dev_dependencies:
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  fonts:
    - family: PFBeauSansPro
      fonts:
        - asset: fonts/PFBeauSansPro-Regular.ttf
